## Login (optioneel)
# Zet LOGIN_PASSWORD om de app te beveiligen met een loginscherm.
# Optioneel: LOGIN_USERNAME voor gebruikersnaam (anders alleen wachtwoord).
# Optioneel: LOGIN_SECRET voor de sessie-cookie; anders wordt LOGIN_PASSWORD gebruikt.
# LOGIN_USERNAME=admin
# LOGIN_PASSWORD=jouw-geheim-wachtwoord
# LOGIN_SECRET=lang-random-geheim-voor-cookie

## Data source
# mock (default) of postgres
DATA_SOURCE=mock

## Postgres connection
# Voorbeeld (lokaal):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dealpublisher?sslmode=disable"
# Voorbeeld (externe N8N/ Fox-deals). SSL wordt automatisch gebruikt voor niet-localhost.
# DATABASE_URL="postgresql://hyperr-n8n:****@34.32.128.18:5432/N8N_XML_Feed_store"
DATABASE_URL=""

## Alleen Fox-brand deals (externe DB met kolom brand)
# Zet DEALS_BRAND=fox om alleen deals met brand = 'fox' te tonen.
# DEALS_BRAND=fox

## generate-kolom is boolean (niet text 'Yes'/'No')
# Zet DEALS_GENERATE_BOOL=true als je tabel generate als bool heeft.
# DEALS_GENERATE_BOOL=true

## Tabelnaam (optioneel, default: deals)
# DEALS_TABLE=deals

## Notificaties (e-mail)
# Resend API key: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxx
# Afzender (optioneel): "Hyperr Poster <notifications@jouwdomein.nl>"
# NOTIFICATION_FROM_EMAIL="Hyperr Poster <onboarding@resend.dev>"

## Cron voor notificaties
# Geheim voor aanroep van /api/cron/notifications (cron-job.org of Netlify Scheduled)
# CRON_SECRET=een-geheim-wachtwoord

## AI (Facebook post genereren)
# OpenAI API key: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...